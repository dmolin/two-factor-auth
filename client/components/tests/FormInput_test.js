import { shallow, mount } from 'enzyme'
import FormInput from '../FormInput'

/*
describe('<FormInput />', () => {
  let subject, $subject

  it('exists', () => {
    subject = shallow(<FormInput />)
  })
})
*/
